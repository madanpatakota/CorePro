import { ApplyYellowColorDirective } from './apply-yellow-color.directive';

describe('ApplyYellowColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyYellowColorDirective();
    expect(directive).toBeTruthy();
  });
});
